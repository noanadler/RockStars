import { toSentence } from 'safe-travels/helpers/to-sentence';
import { module, test } from 'qunit';

module('Unit | Helper | to sentence');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toSentence([42]);
  assert.ok(result);
});
